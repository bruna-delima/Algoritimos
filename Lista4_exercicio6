#include <stdio.h>

int somatorio (int n) {
    int s = 0;
    
    for (int i = 1; i <= n; i++)
    s = s + 5 * i * i + 2 * i + 8;
    
    return s;
} 

int main () {
    int n;
    
    printf("Digite o valor de n: ");
    scanf("%d", &n);
    
    printf("Resultado %d", somatorio (n));
}
